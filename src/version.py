"""Версия 1C MCP Bridge.

Единый источник версии для сервера и диагностики.
При сборке установщика (GitHub Actions / Inno Setup) значение может быть
переопределено переменной окружения APP_VERSION (например "0.5.4");
по умолчанию — последний опубликованный релиз.
"""

import os

VERSION = (os.environ.get("APP_VERSION", "") or "0.5.3").lstrip("v")
