<div align="center">

<img src="assets/icon-preview.png" alt="1C MCP Bridge" width="128" />

# 1C MCP Bridge

**Подключите AI-ассистента (Claude, Qwen, Kimi, Reasonix, ChatGPT) к 1С:Предприятию и работайте с базой как с источником данных через диалог.**

[![GitHub release](https://img.shields.io/github/v/release/dimax27/1c-mcp-bridge)](../../releases/latest)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)
[![Windows](https://img.shields.io/badge/Windows-10%20%7C%2011%20%7C%20Server-blue.svg)]()

</div>

---

## Что это

`1c-mcp-bridge` — сервер [Model Context Protocol](https://modelcontextprotocol.io/),
который подключает AI-ассистентов (Claude Desktop, Qwen Desktop, Kimi Desktop,
Reasonix, ChatGPT Desktop) напрямую к одной или нескольким информационным базам
1С через COM-коннектор. AI-ассистент получает пять инструментов
(`execute_query`, `describe_object`, `list_metadata`, `get_object_by_ref`,
`list_databases`) и работает с базой как с обычным источником данных:
формулирует запрос на языке запросов 1С, исполняет, возвращает результат,
считает аналитику.

```
┌──────────────────────────┐   stdio / HTTP   ┌──────────────────┐   COM   ┌──────────────────┐
│ Claude / Qwen / Kimi /   │ ────────────────► │ MCP-сервер (Py)  │ ──────► │ 1С:Предприятие   │
│ Reasonix / ChatGPT       │                   └──────────────────┘         └──────────────────┘
└──────────────────────────┘
```

У моста **два транспорта**:

* **stdio** — для Claude Desktop, Kimi Desktop, Reasonix (прописывается
  установщиком автоматически);
* **streamable-HTTP** на `http://127.0.0.1:8000/mcp/<токен>` — для Qwen Desktop
  и ChatGPT Desktop (песочница MSIX не даёт stdio-процессам работать с COM).
  HTTP-сервер запускается без окна (ярлык **«1C Bridge (HTTP-сервер)»**),
  слушает только локальный адрес, доступ защищён секретным путём-токеном.

## Кому это нужно

* **Бухгалтеру / финансовому аналитику**, который хочет сказать «покажи топ-10
  покупателей за апрель» и получить таблицу.
* **Разработчику 1С**, которому удобно проверить структуру конфигурации без
  захода в Конфигуратор.
* **Команде с несколькими базами** (УТ + БП + ЗУП) — AI-ассистент видит все
  настроенные базы сразу, для каждой можно дать осмысленное описание, и он сам
  выбирает правильную для конкретного вопроса.

## Что умеет

| Среда | Поддержка |
|---|---|
| Платформа 1С:Предприятие | 8.2, 8.3, 8.5 |
| Файловая база (.1CD) | да |
| Клиент-серверная база (1С-сервер) | да |
| Аутентификация Windows (OS-auth) | да |
| Аутентификация средствами 1С | да (пароль шифруется DPAPI) |
| Несколько баз одновременно | да |
| ОС | Windows 10 / 11 / Server 2016+ |

> **Только чтение.** Все операции строго read-only: `execute_query` использует
> объект `Запрос`, который не модифицирует данные; инструменты помечены
> `read_only_hint=True` для совместимых клиентов.

## Установка

1. Скачайте `1cMcpBridge-Setup-<версия>.exe` со страницы
   [Releases](../../releases/latest) (актуальную версию см. на бейдже выше).
2. Запустите от имени администратора (нужно для регистрации COM-коннектора).
3. Пройдите шаги мастера: тип базы (файловая или клиент-серверная), параметры
   подключения, краткое имя базы и тест подключения.

<table>
  <tr>
    <td><img src="assets/screenshots/installer-mode.png" alt="Тип ИБ" width="450" /></td>
    <td><img src="assets/screenshots/installer-params.png" alt="Параметры подключения" width="450" /></td>
  </tr>
  <tr>
    <td align="center"><i>Выбор типа базы — файловая или клиент-серверная</i></td>
    <td align="center"><i>Параметры подключения и краткое имя базы</i></td>
  </tr>
  <tr>
    <td colspan="2"><img src="assets/screenshots/installer-test.png" alt="Тест подключения" width="900" /></td>
  </tr>
  <tr>
    <td colspan="2" align="center"><i>Тест подключения прямо из мастера — можно убедиться, что 1С отзывается</i></td>
  </tr>
</table>

4. Установщик автоматически:
   * **останавливает старый HTTP-сервер** моста (точечно, по путям установки —
     посторонние процессы с тем же именем скрипта не затрагиваются);
   * **сохраняет HTTP-токен** (не меняет его при обновлении — не ломаются
     сохранённые URL в Qwen/ChatGPT/Tunnel);
   * настраивает MCP-клиенты: Claude Desktop (`claude_desktop_config.json`),
     Qwen Desktop (npx-лаунчер), Kimi Desktop (`mcp_config.json`),
     Reasonix (`.mcp.json`), ChatGPT Desktop (`%USERPROFILE%\.codex\config.toml`);
   * если HTTP-сервер работал до обновления — **запускает его заново и
     выполняет настоящий MCP health-check** (5 инструментов + `list_databases`);
     при провале установка сообщает об ошибке, а не «успех».
5. Перезапустите MCP-клиенты: Claude Desktop — правый клик по иконке в трее →
   **Quit**; Qwen/Kimi — закрыть и запустить заново; ChatGPT Desktop — полный
   выход из трея и повторный запуск. В новом чате появится сервер `1c-bridge`.

<div align="center">
<img src="assets/screenshots/claude-connector.png" alt="1c-bridge в AI-ассистенте" width="500" />
</div>

6. Дальше задавайте вопросы:

> Покажи топ-10 покупателей по выручке за апрель 2026 с использованием регистра
> Продажи. Сначала посмотри его структуру через describe_object.

AI-ассистент сам пройдёт по инструментам, составит и выполнит запрос, вернёт таблицу.

## Подключение к ChatGPT Desktop

ChatGPT Desktop (OpenAI Codex, MSIX) не может запускать stdio-MCP-серверы с
COM/DPAPI внутри своей песочницы, поэтому мост подключается к нему **по HTTP**
через общий `%USERPROFILE%\.codex\config.toml` (этот файл использует и Codex CLI/IDE).

Установщик настраивает это автоматически (если найден `config.toml`):
в `[mcp_servers.1c-bridge]` записывается

```toml
enabled = true
url = "http://127.0.0.1:8000/mcp/<токен>"
```

Патчер точечный: ваши настройки (`tool_timeout_sec`, `required`,
`enabled_tools`, approval-режимы, подсекции `.tools.*`) сохраняются;
удаляются только несовместимые ключи stdio (`command`/`args`/`cwd`) и старая
подсекция `.env`.

### Важно: выберите правильный режим ChatGPT Desktop

`1c-bridge`, подключённый как локальный MCP-сервер через Streamable HTTP,
следует использовать в новом разговоре одного из следующих режимов:

- **ChatGPT → Work**;
- **Codex**.

В обычном режиме **Chat** локальные MCP-инструменты `1c-bridge` могут не
подключаться к разговору, даже если в разделе:

`Настройки → Плагины → MCP`

сервер отображается со статусом **«Включено»**. Статус «Включено» означает,
что конфигурация MCP-сервера найдена и разрешена, но не гарантирует, что его
инструменты доступны в уже открытом или обычном Chat-разговоре.

#### Пошаговая проверка

1. Запустите или перезапустите ярлык **«1C Bridge (HTTP-сервер)»**.
2. Полностью завершите ChatGPT через меню **Файл → Выход** или значок в трее.
3. Снова откройте ChatGPT Desktop.
4. Создайте **новый** разговор.
5. Выберите `ChatGPT → Work` либо `Codex`.
6. Введите команду `/mcp` и убедитесь, что в списке есть `1c-bridge — Включено`.
7. Закройте список и отправьте обычный запрос без символа `@`, например:

   > Используй MCP-сервер 1c-bridge, вызови list_databases и покажи результат.

При успешном подключении ChatGPT выполнит инструментальный вызов
`1c-bridge → list_databases`.

#### Не используйте `@1c-bridge` для проверки локального MCP

При вводе `@1c` ChatGPT Desktop может показать найденные программы и ярлыки
Windows с подписью **«Использование компьютера»** (например 1С, ярлык запуска
HTTP-сервера). Это объекты **Computer Use**, а не MCP-инструменты
`list_databases`, `execute_query`, `list_metadata`, `describe_object` и
`get_object_by_ref`. Для проверки MCP используйте команду `/mcp`, а затем
обычный текстовый запрос.

#### Сообщение «Аутентификация не поддерживается»

Если под `1c-bridge` в списке `/mcp` показано:

> Аутентификация не поддерживается

это не является ошибкой подключения. `1c-bridge` использует секретный токен
в пути HTTP endpoint, а не интерактивную OAuth-аутентификацию. Основной
индикатор состояния — строка **«Включено»** и успешный вызов инструмента.

Что нужно сделать вручную (кратко):

1. Запустите ярлык **«1C Bridge (HTTP-сервер)»** (или дождитесь автозапуска —
   см. [HTTP-сервер и ярлыки](#http-сервер-и-ярлыки)).
2. В ChatGPT Desktop: **Плагины → MCP → Серверы** — включите `1c-bridge`
   (если он в списке, но выключен).
3. Полностью завершите приложение (трей → Quit) и запустите заново, создайте
   новый разговор в режиме **Work** или **Codex** (обычный Chat не использует
   локальные MCP).
4. Проверьте: `/mcp` → убедитесь, что `1c-bridge` есть и включён, закройте
   список и спросите: «Используй MCP-сервер 1c-bridge, покажи список
   информационных баз».

Ручная настройка (если `config.toml` ещё не создан):

```powershell
# токен и URL
$token = (Get-Content "$env:ProgramData\1cMcpBridge\.http_token" -Raw).Trim()
"http://127.0.0.1:8000/mcp/$token"
```

```toml
[mcp_servers.1c-bridge]
enabled = true
url = "http://127.0.0.1:8000/mcp/<токен>"
```

### ChatGPT Web и Secure MCP Tunnel

Чат на **chatgpt.com** не может достучаться до `127.0.0.1` — запросы идут через
облако OpenAI, и локальный сервер им недоступен. Для веб-версии нужен удалённый
endpoint (публичный HTTPS или **Secure MCP Tunnel** — официальный механизм
OpenAI для доступа облачного ChatGPT к локальным MCP-серверам). Tunnel
устанавливает исходящее HTTPS-соединение с OpenAI и пересылает MCP-вызовы на
локальный `http://127.0.0.1:8000/mcp/<токен>`. Входящий порт в интернет
открывать не нужно; в разделе Плагины при создании MCP-сервера выбирается тип
Tunnel с этим URL. После подключения в веб-чате выполните **Scan Tools /
Refresh**, чтобы приложение увидело инструменты.

Открывать локальный HTTP-порт напрямую в интернет не рекомендуется.

## Подключение к Qwen Desktop

Qwen Desktop разрешает только `npx`/`uvx` для stdio MCP-серверов. Установщик
автоматически прописывает Qwen через npx-лаунчер моста
(`command = "npx"`, работает из коробки) — ручная настройка HTTP не нужна.

Если настраиваете Qwen вручную через HTTP — используйте **URL с токеном**:
`/mcp` без токена сервер не обслуживает (404).

1. Запустите ярлык **«1C Bridge (HTTP-сервер)»** из меню Пуск.
2. Узнайте токен и соберите URL:

   ```powershell
   $token = (Get-Content "$env:ProgramData\1cMcpBridge\.http_token" -Raw).Trim()
   "http://127.0.0.1:8000/mcp/$token"
   ```

3. В Qwen Desktop: Настройки → MCP → Добавить:
   - Тип: **StreamableHTTP**
   - URL: полученный на шаге 2 (вида `http://127.0.0.1:8000/mcp/<токен>`)

## Другие клиенты (Claude, Kimi, Reasonix)

| Клиент | Файл конфигурации | Транспорт |
|---|---|---|
| Claude Desktop | `%APPDATA%\Claude\claude_desktop_config.json` | stdio |
| Kimi Desktop | `%APPDATA%\kimi-desktop\mcp_config.json` | stdio |
| Reasonix | `%APPDATA%\reasonix\global-workspace\.mcp.json` | stdio |

Все они настраиваются установщиком автоматически: в конфиг добавляется
`1c-bridge` с командой запуска venv-python и `mcp_server_1c.py`. После
установки достаточно перезапустить клиент.

## HTTP-сервер и ярлыки

В меню «Пуск → 1C MCP Bridge» — четыре служебных ярлыка:

| Ярлык | Что делает |
|---|---|
| **1C Bridge (HTTP-сервер)** | Запускает HTTP-сервер в фоне (без окна); токен и путь к базе читает из `%PROGRAMDATA%\1cMcpBridge` при запуске |
| **Остановить HTTP-сервер** | Останавливает только процесс этой установки и освобождает порт 8000 |
| **Перезапустить HTTP-сервер** | Останавливает, запускает заново и выполняет **настоящий MCP health-check** (5 инструментов + `list_databases`); при провале сообщает `RESTART_FAIL` |
| **Статус HTTP-сервера** | Показывает: слушает ли порт 8000, PID, наличие токена, секцию `1c-bridge` в `~/.codex/config.toml`, хвост журнала |

HTTP-сервер слушает только `127.0.0.1:8000`. Журнал — 
`%PROGRAMDATA%\1cMcpBridge\http-server.log` (ротация 5 МБ × 3): туда пишутся
запуск, ошибки импорта/порта и медленные запросы (SHA-256 запроса, без текста).

**Автозапуск при входе в систему** — двумя способами:

* Планировщик заданий: задача **«1C MCP Bridge HTTP»** (создаётся при
  установке), триггер «при входе пользователя», запуск от имени пользователя
  (пароли DPAPI привязаны к пользователю), `/RL LIMITED`;
* либо ярлык **«1C Bridge (HTTP-сервер)»** в `shell:startup`.

## Несколько баз и заметки для AI

В `%PROGRAMDATA%\1cMcpBridge\databases.json` — список баз. Каждая запись:

```json
{
  "enabled": true,
  "description": "УТ 10.3 — управленческая база",
  "progid": "V83.COMConnector",
  "connection_string": "Srvr=192.168.0.35;Ref=ut10;Usr=\"Кувыкин Д.А.\"",
  "notes": "Основная управленческая база ...",
  "dll_path": "C:\\Program Files\\1cv8\\8.3.27.1859\\bin\\comcntr.dll",
  "credential": { "provider": "dpapi-current-user", "blob": "..." }
}
```

Редактировать список удобнее через **«1C Bridge Manager»** (меню Пуск):
добавление/удаление баз, тест подключения, заметки для AI. Пароль хранится в
поле `credential` (DPAPI, привязан к текущему пользователю Windows) и не
попадает в `connection_string`. Manager **сохраняет** credential при
редактировании базы: пустое поле пароля = «оставить прежний»; новый пароль
заменяет credential; переход на Windows-аутентификацию или смена пользователя
сбрасывают его. После сохранения Manager сам перезапускает HTTP-сервер
(если он запущен), чтобы базы подхватились сразу.

`description` и `notes` передаются ассистенту через `list_databases` — именно
по ним он выбирает, какую базу использовать для вопроса.

## Безопасность

* **HTTP-токен** — случайная строка, хранится в `%PROGRAMDATA%\1cMcpBridge\.http_token`.
  Доступ к MCP-серверу возможен только по секретному пути `/mcp/<токен>`:
  `/mcp` без токена возвращает 404. Токен **не пишется** в журналы, VBS-лаунчер
  читает его из файла при запуске, в логах установки — только маска `/mcp/<token>`.
* **Токен не меняется при переустановке** — сохранённые URL (Qwen, Tunnel,
  сторонние клиенты) продолжают работать. Ротация вручную: остановить сервер
  (ярлык «Остановить HTTP-сервер»), удалить `.http_token`, повторно запустить
  установщик (он создаст новый токен), обновить ручные URL в Qwen/Tunnel и
  секцию `url` в `~/.codex/config.toml` (`clients_config.py patch-codex`),
  затем запустить сервер.
* **Пароли баз** — DPAPI-шифрование (текущий пользователь Windows). Пароль не
  хранится в открытом виде ни в конфигах, ни в настройках MCP-клиентов:
  `databases.json` держит только `credential` (DPAPI-blob) и строку соединения
  без `Pwd=`. При первой установке установщик временно пишет `Pwd=` в
  `databases.json` (иначе первое подключение невозможно), а при первом запуске
  сервер автоматически мигрирует его в DPAPI и убирает из строки соединения —
  до этого момента файл защищён ACL (только для интерактивного пользователя).
  Пароль, введённый в Manager, шифруется сразу и в открытом виде никуда не
  попадает.
  (клиентам передаётся только команда запуска сервера).
* **Только чтение**: `execute_query` использует объект `Запрос` 1С — данные не
  модифицируются; инструменты помечены `read_only_hint=True`.
* **Остановка процессов** при переустановке — только для процесса текущей
  установки (по точному пути `mcp_server_1c_http.py` и python из её `.venv`).

### Модель угроз

Бридж запущен на локальной машине и обслуживает одного пользователя 1С.
Таблица ниже перечисляет векторы, актуальные при использовании MCP-клиентов
(Codex Writer, Claude, Codex CLI и др.) в многопользовательских или
shared-сценариях.

| Вектор | Риск | Статус |
|---|---|---|
| Прямой доступ к `127.0.0.1:8000` | Только localhost; токен в URL | ✓ Закрыто |
| MCP-клиент в чужом проекте/чате | Может вызвать инструменты 1C от имени того же пользователя | ⚠️ Документирован |
| Prompt injection через MCP-инструменты | Инструменты read-only, 1С-запросы без модификации | ✓ Низкий |
| RAG / Vector Store / File Search | Контекст из чужих файлов может направить модель на вызов 1C-инструментов | ⚠️ Документирован |
| DPAPI-credential кража | Привязан к пользователю Windows | ✓ Закрыто |

**Пояснения к ⚠️-векторам:**

MCP-клиент (например Codex Writer) подключается к бриджу как обычный
MCP-клиент — он видит все 5 инструментов (`list_databases`, `execute_query` и
др.) и может вызывать их, если модель решит это сделать. Когда вы открываете
общий проект или чат, созданный другим пользователем, prompt/RAG-контекст
этого проекта может содержать инструкции, косвенно приводящие к вызову
инструментов 1C. Поскольку бридж обслуживает единственного пользователя 1С
(чьи учётные данные записаны в `databases.json`), результат такого вызова
будет выполнен от имени этого пользователя.

**Смягчение:** ограничьте права 1С-пользователя, под которым работает бридж
(только необходимые справочники/регистры, без модификации данных). Проверьте
настройки конкретного MCP-клиента: например, Codex Writer позволяет отключать
MCP-серверы для отдельных проектов (`.codex/instructions.md`).

## Диагностика проблем

1. **Ярлык «Статус HTTP-сервера»** — порт, токен, секция ChatGPT, хвост журнала.
2. **Журнал** `%PROGRAMDATA%\1cMcpBridge\http-server.log` — если сервер «молча»
   не стартует (занят порт, не найден модуль, битый `databases.json`), причина
   будет там. Файл в **UTF-8** — читайте с явной кодировкой:
   ```powershell
   Get-Content "$env:ProgramData\1cMcpBridge\http-server.log" -Encoding UTF8 -Tail 50
   ```
3. **detect_1c.ps1** (в папке установки) — сводка: платформы 1С, Python,
   настройки MCP-клиентов.
4. **`@1c-bridge` не виден в чате?** Перезапустите клиент полностью (трей →
   Quit), в ChatGPT Desktop проверьте «Плагины → MCP → Серверы» (включён ли
   `1c-bridge`) и выполните Scan Tools/Refresh.
5. **Глубокая проверка COM/метаданных** — единый healthcheck с пробой каждой
   включённой базы (tools/list + list_databases + COM-подключение через
   `list_metadata` с заведомо несуществующим фильтром):
   ```powershell
   $env:ONEC_HTTP_TOKEN = (Get-Content "$env:ProgramData\1cMcpBridge\.http_token" -Raw).Trim()
   & "$env:ProgramFiles\1cMcpBridge\.venv\Scripts\python.exe" "$env:ProgramFiles\1cMcpBridge\healthcheck.py" --com
   ```
   Успех: `HEALTH_COM_OK`, код 0. При ошибке выводится
   `HEALTH_COM_FAIL databases=[...]` и код 3.
   Установщик и ярлык «Перезапустить HTTP-сервер» используют **базовый**
   health-check (без `--com`): HTTP, 5 инструментов, `list_databases` со
   списком баз. Глубокая COM-проверка — только вручную с `--com`; для одной
   базы — `--com --database <ключ>`.

## Сборка из исходников

```powershell
git clone https://github.com/dimax27/1c-mcp-bridge.git
cd 1c-mcp-bridge

# тесты и линт (release-gate: эти команды выполняются в CI перед сборкой)
python -m compileall -q src installer
ruff check src installer tests --ignore E501,E722
python -m pytest tests -q

# сборка установщика: build.ps1 запекает версию в src/version.py,
# запускает Inno Setup и восстанавливает исходный файл (локально и в CI)
.\installer\build.ps1 -Version "1.2.3"
```

Готовый `.exe` появится в `.\dist\` с именем `1cMcpBridge-Setup-<версия>.exe`.
Сервер, установленный из исходников без сборки, честно показывает `dev`.

### Тесты

`python -m pytest tests -q` — 50+ тестов: юнит (парсер конфигов, TOML-патчер
Codex, DPAPI-логика Manager), HTTP smoke (реальный сервер: токен-маршрут, 404
без токена, read-only аннотации, healthcheck, занятый порт) и installer-тесты
(остановка только целевого процесса, неразрушающий патч, редактирование
stdout).

## Roadmap

* **v0.5.21** *(текущая)* — Production-релиз: модель угроз (5 векторов
  безопасности), документация режимов ChatGPT Desktop (Work/Codex vs Chat,
  @1c-bridge ≠ MCP, Secure MCP Tunnel), подсказка на финальном экране
  установщика через `[Messages]`; починка CI/Build (версии actions,
  httpx2→httpx, Python в build-джобе, `[Messages]` вместо Pascal).
* **v0.5.15** — санитизация всех диагностических веток healthcheck,
  healthcheck `--com`: error-ответ и debug-traceback
  (`ONEC_HEALTHCHECK_DEBUG=1`) проходят через `redact_secrets()` (тесты на
  фейковом клиенте); задача Планировщика привязывается к интерактивному
  пользователю (`Win32_ComputerSystem.UserName`) и всегда перерегистрируется
  с `-Force`; `build.ps1`: валидация версии (семвер), экранирование кавычек,
  `py_compile` запечённого `version.py`, восстановление прежнего
  `APP_VERSION`; документация уточняет, что `Pwd=` из мастера установки
  временно лежит в `databases.json` и мигрируется в DPAPI при первом запуске
  сервера.
* **v0.5.13** — безопасность healthcheck: секреты (токен `/mcp/<token>`,
  `Pwd="***"`) редактируются в traceback (раньше могли попасть в install.log),
  `check_databases_payload` принимает любой JSON-ответ, COM-пробы пишут
  краткую причину в stderr; единый `installer/build.ps1` (запекает версию и
  восстанавливает), установщик создаёт задачу Планировщика автозапуска,
  корректная инструкция ротации токена.
* **v0.5.12** — healthcheck `--com` ужесточён: ключи баз
  экранируются `ensure_ascii` (ASCII-вывод при кириллических ключах), пустой/
  некорректный список баз или отсутствие `default_database` — ошибка (код 2),
  `--database` только вместе с `--com`; версия исходников перестала
  «отставать» от релиза (fallback `dev`), ruff-гейт покрывает и `tests`.
* **v0.5.11** — healthcheck `--com`: ASCII-safe проверка COM/метаданных
  каждой базы, `--com --database <ключ>`, инструкция чтения UTF-8-журнала.
* **v0.5.10** — единая кодировка UTF-8: журналы и их чтение
  (`-Encoding UTF8`), роли процессов (лаунчер/рабочий) в статусе.
* **v0.5.8** — починка CI (кодировка вывода PowerShell в тесте,
  невалидный SHA `actions/setup-python` в ci.yml), надёжный lifecycle
  HTTP-сервера (строгий MCP health-check в установщике и ярлыке «Перезапустить»,
  проверка результата остановки), единый `healthcheck.py`, DPAPI-credential не
  стирается при смене аутентификации, release-gate (тесты + ruff) перед сборкой.
* **v0.5.6** — автозапуск HTTP-сервера при входе (Планировщик заданий), ярлыки
  «Остановить/Перезапустить/Статус HTTP-сервера».
* **v0.5.5** — переименован ярлык «1C Bridge (HTTP-сервер)» (общий для Qwen и
  ChatGPT).
* **v0.5.4** — bootstrap-журнал HTTP-сервера (ошибки запуска/импорта видны в
  `http-server.log`, ротация 5 МБ×3), VBS читает токен из файла при запуске,
  read-only аннотации инструментов, единая версия (`src/version.py`),
  HTTP smoke-тесты в CI.
* **v0.5.3** — лог-файл HTTP-сервера, пины зависимостей, раздел Desktop vs Web
  в README, smoke-тест.
* **v0.5.1** — исправлен установщик (докопированы модули
  `config/credentials/query_timeout`), поддержка ChatGPT Desktop (HTTP вне
  песочницы MSIX), реальный токен-путь у HTTP-сервера.
* **v0.5.0** — DPAPI-шифрование паролей, HTTP-токен, CI + тесты, lock-файл,
  сужение DLL-регистрации, логгирование долгих запросов.
* **v0.6.0** *(планируется)* — изоляция COM в worker-процессах с настоящим
  тайм-аутом запросов, подпись установщика, английский README.

## Лицензия

[MIT](LICENSE) © 2026 Koovykin D.
